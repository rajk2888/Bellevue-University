# Parallel research returns

The active goal is checkpoint `P17` at Git revision `abc123`. Two authorized
routes started from that same checkpoint.

- Route A returns a proof note and proposes replacing `notes/result.md` with a
  conditional lemma.
- Route B returns a counterexample note and proposes replacing the same path
  with a restricted theorem.

The interface displays Route B first, but neither return contains trustworthy
wall-clock ordering. Both include hashes of their raw artifacts. Reconcile the
returns without discarding evidence, then state how the program should proceed.
